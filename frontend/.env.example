# Frontend environment variables
# Copy to .env and fill in

# Backend API URL — leave as /api if using Vite proxy (recommended for dev)
VITE_API_URL=/api

# Or point directly to backend for production:
# VITE_API_URL=https://api.yourdomain.com/api
